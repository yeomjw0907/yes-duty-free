---

## 1) 회원가입 및 상점 준비

- [ ] PortOne 회원가입 완료
- [ ] 관리자 콘솔 로그인 가능 확인
- [ ] 결제 연동할 상점(스토어) 생성/선택 완료
- [ ] 팀 내 담당자(정산/운영) 계정 권한 공유 완료

메모:
- 먼저 샌드박스(테스트) 기준으로 진행하면 안전합니다.

---

## 2) 결제 연동 키 발급 (필수)

개발팀 전달이 필요한 키:

- [ ] `PORTONE_KEY` 발급
- [ ] `PORTONE_SECRET` 발급

전달 방법:
- 사내 보안 채널(비공개 비밀번호 관리자, 보안 메신저 등)로 전달
- 메일/메신저 공개방에 평문으로 붙여넣지 않기

---

## 3) 웹훅(Webhook) 설정 (필수)

웹훅은 결제 성공/실패/환불 결과를 우리 시스템으로 보내는 기능입니다.

- [ ] 웹훅 시크릿 발급 (`PORTONE_WEBHOOK_SECRET`)
- [ ] 웹훅 URL 등록

등록할 URL:

`https://kpruididwshmhyxxydwf.supabase.co/functions/v1/portone-webhook`

개발팀 전달이 필요한 값:
- [ ] `PORTONE_WEBHOOK_SECRET`

---

## 4) 결제 채널/PG 연결 확인

- [ ] 테스트 가능한 결제 채널 연결 상태 확인
- [ ] 결제수단(카드 등) 테스트 가능 상태 확인
- [ ] 샌드박스 테스트 결제 가능한지 확인

주의:
- 실운영(라이브) 전환 전에는 샌드박스에서 먼저 검증합니다.

---

## 5) 실운영 전환 시 추가로 필요한 항목

- [ ] 사업자/정산 정보 등록
- [ ] 라이브 키 발급/승인 상태 확인
- [ ] 라이브 웹훅 URL/시크릿 재검증
- [ ] 내부 운영팀(CS/정산) 공지 완료

---

## 6) 최종 전달 체크 (개발팀에 전달)

아래 3개가 전달되면 개발팀이 서버 설정을 마무리할 수 있습니다.

- [ ] `PORTONE_KEY`
- [ ] `PORTONE_SECRET`
- [ ] `PORTONE_WEBHOOK_SECRET`

권장 전달 형식:

```txt
[PortOne 전달값 - Sandbox]
PORTONE_KEY=...
PORTONE_SECRET=...
PORTONE_WEBHOOK_SECRET=...
```

---
